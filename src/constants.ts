import { IPosition } from "./types";

export const GAME_SIZE = 8;
export const ITEMS_WIDTH = 4;
export const ITEMS_HIGHT = 3;
export const EMPTY_POSITION: IPosition = Object.freeze({ x: -1, y: -1 });
